/**
 * Booking Filters Hook
 *
 * Re-exported from the original location for feature-based architecture.
 * Filters and sorts bookings based on various criteria.
 */

export { useBookingFilters } from '@/hooks/bookings/useBookingFilters';
export type { BookingFilters } from '@/hooks/bookings/useBookingFilters';
